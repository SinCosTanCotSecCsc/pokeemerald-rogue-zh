# 简体中文汉化说明

本仓库是 [pokeemerald-rogue](https://github.com/Pokabbie/pokeemerald-rogue) 的简体中文汉化版。
中文渲染方案移植自 [Xzonn/PretFRLG](https://github.com/Xzonn/PretFRLG)。

设计目标是**让上游更新可以低成本同步**，因此译文并不写进上游源码，而是外置成独立的数据文件，
由构建工具在编译期注入。上游源码本身一行未改，`git merge upstream` 基本不会冲突。

- [一、编译](#一编译)
- [二、上游更新后如何操作](#二上游更新后如何操作)
- [三、修改或新增译文](#三修改或新增译文)
- [四、原理与文件清单](#四原理与文件清单)

---

## 一、编译

### 1. 前置依赖

| 依赖 | 说明 |
|---|---|
| [devkitARM](https://devkitpro.org/wiki/Getting_Started) | 需设置环境变量 `DEVKITARM`（devkitPro 安装器会自动设好） |
| poryscript | 仓库不附带，用 `sh init_deps.sh` 下载 |
| 构建工具 | `make tools` 或首次 `make` 时自动构建 |

首次编译：

```bash
git clone <本仓库地址>
cd pokeemerald-rogue
sh init_deps.sh          # 下载 poryscript 等第三方依赖
make -j$(nproc)          # 编译
```

产物为 `pokeemerald.gba`。完整的工具链安装说明见 [INSTALL.md](INSTALL.md)。

编译耗时参考：全量约 30 秒（8 核）。

### 2. 确认汉化生效

启动游戏后主菜单第一项应为「新游戏」。

也可以直接检查 ROM 内容 —— 「新游戏」的编码是 `0E 4D 0F 7C 0D DB`：

```bash
python3 -c "
d = open('pokeemerald.gba','rb').read()
print('汉化生效' if d.find(bytes.fromhex('0E4D0F7C0DDB')) >= 0 else '未生效')
"
```

### 3. 关闭汉化

有时需要拿原版英文行为做对照（例如排查某个显示问题是不是汉化引起的）：

```bash
make clean
OMP_TRANSLATION= make -j$(nproc)
```

`OMP_TRANSLATION` 设为空串表示本次构建不翻译，其余行为完全一致。
也可以指定其它翻译表文件：`OMP_TRANSLATION=translations/繁体.txt make -j8`。

### 4. 分支

`pokeemerald-rogue` 有多个并行分支。汉化依赖的上游改动点（`tools/preproc/`、
`gflib/text.c` 的字体分发、`src/naming_screen.c` 的输入框绘制）在各分支间**完全一致**，
因此同一套汉化可以直接套用。

| 分支 | 状态 |
|---|---|
| `vanilla`、`vanilla-dev`、`feature-rebalance-vanilla` | 关键文件与 `vanilla` 相同，可直接套用 |
| `expansion` | **已实测**：cherry-pick 零冲突，编译通过，1665 条译文中 1663 条原样生效 |
| `expansion-dev`、`expansion-int`、`feature-rebalance-expansion`、`feature-za-expansion`、`sandbox-expansion` | 钩子点与 `expansion` 相同，预期可直接套用（未逐一实测） |
| `sandbox-vanilla` | `tools/preproc/charmap.cpp` 与 `vanilla` 不同，需先确认 |
| `symbols` | 只含 `pokeemerald.sym`，不是游戏分支 |

为一个新分支套用汉化：

```bash
git checkout -b zh-hans-<分支> origin/<分支>
git cherry-pick <汉化提交>              # 预期零冲突
python3 tools/translation_check.py      # 检查陈旧条目
make -j$(nproc)
```

---

## 二、上游更新后如何操作

### 步骤

```bash
git remote add upstream https://github.com/Pokabbie/pokeemerald-rogue.git   # 仅首次
git fetch upstream
git merge upstream/<你的分支>            # 1. 合并
python3 tools/translation_check.py       # 2. 必须执行，见下
make -j$(nproc)                          # 3. 编译
```

### 第 2 步为什么不能省

这是本机制**唯一会静默失败**的地方。

翻译表按「源文本」精确匹配。如果上游改写了某句文案（哪怕只加一个标点），原来的译文
就匹配不上了，游戏会**回退显示英文**，而**编译不会报任何错**。实测：

```
上游把 "New Game" 改成 "New Game!"
→ 构建 exit=0，无警告
→ ROM 中「新游戏」消失，变成 "New Game!"
```

`translation_check.py` 就是用来发现这件事的，它的输出形如：

```
陈旧条目 2 条（原文本在源码中已不存在）:
   translations/zh_CN.txt:1436: Your party's full!
   translations/zh_CN.txt:1637: {STR_VAR_1} has survived poisoning…\p\n

上游若改写了这些文案，对应译文已静默回退为英文（构建不会报错）。
处理方式：按新文案补一条表项，再删掉陈旧那条。
```

发现陈旧条目后：在源码里搜一下新文案长什么样，把这一行改写成新的原文，
译文可沿用或微调。

### 预期冲突

合并时预计只有 2 个文件各 1 处冲突，且都是「两边都保留」型（因为汉化的改动几乎都是纯新增）：

| 文件 | 冲突原因 | 处理 |
|---|---|---|
| `charmap.txt` | 双方都在文件末尾追加内容 | 两段都保留 |
| `src/naming_screen.c` | 上游改动 `DeleteTextCharacter` 附近的逻辑 | 两边的改动都保留 |

其余大量文件（包括文案最多的 `src/strings.c`）都是自动合并。

### 上游新增的文案

会在游戏里显示英文，**不会导致构建失败**。可以照常游玩，之后再按
[第三节](#三修改或新增译文) 逐条补译。

---

## 三、修改或新增译文

所有译文集中在**一个文件**：`translations/zh_CN.txt`。

### 格式

```
"英文原文" = "中文译文"
```

- 两侧都是字符串字面量的内容，**不含外层引号**（外层引号由文件本身提供）
- 一行一条，`#` 或 `@` 开头的行和空行会被忽略
- 原文重复会报错（同一句英文只能有一个译文，见下方「注意事项」）

改完直接 `make -j$(nproc)` 即可，Makefile 已配置好依赖，会自动重编所有文本。

### 修改已有译文

找到对应行改掉即可：

```
"NEW GAME" = "新游戏"
```

### 新增译文

**第 1 步**：在源码里找到英文原文，注意要**逐字照抄**，包括标点和转义：

```bash
grep -rn 'Press START to begin' src/ data/
```

**第 2 步**：在 `translations/zh_CN.txt` 末尾加一行：

```
"Press START to begin." = "按 START 键开始。"
```

**第 3 步**：校验并编译：

```bash
python3 tools/translation_check.py
make -j$(nproc)
```

### 书写规则

**1. 转义与占位符必须与原文一致**

```
"CONGRATS!\nYou win."          →  "\n" 表示换行，必须保留
"{PLAYER} got {STR_VAR_1}!"    →  {PLAYER} 等占位符原样保留
"{A_BUTTON}OK"                 →  按键提示同理
```

占位符的**顺序**可以随中文语序调整，但数量和拼写必须一致：

```
"{STR_VAR_1} is selected."  →  "已选择 {STR_VAR_1}。"     ✅
"{STR_VAR_1} is selected."  →  "已选择。"                 ❌ 占位符丢了
```

校验器会检查这一项。

**2. 只能用字库收录的字符**

字库是 **GB2312 全集**（6763 汉字 + 12 个全角标点 `：；？！，．、。《》—～`）。
用到字库之外的字（如「囧」「喆」「昇」「玥」）会导致**编译失败**：

```
error: unknown character U+9F98
```

这不是运行期问题，编译时就会拦住。遇到报错时换个常见同义字，或参考
[新增汉字](#新增汉字)。

另外注意：**全角括号 `（）` 不在字库内**，请用半角 `()`。

**3. 名称类字符串有字节上限**

道具名、招式名、宝可梦名等有硬性长度限制，中文一个字占 2 字节：

| 类别 | 上限 | 最多汉字数 |
|---|---|---|
| 道具名 | 16 字节 | 8 |
| 招式名 / 特性名 | 12 字节 | 6 |
| 宝可梦名 / 昵称 | 10 字节 | 5 |
| 玩家名 | 7 字节 | 3 |
| 盒子名 | 8 字节 | 4 |
| 枢纽名 | 15 字节 | 7 |

超限会被校验器报出。超长会被截断，显示残缺。

**4. 纯中文长句不会自动换行**

poryscript 只在**空格**处断行。中文没有空格，所以 `.pory` 脚本里的长句需要手动加 `\n`：

```
msgbox(format("这是一句很长的话，需要手动断行\n否则会冲出文本框。"))
```

`src/strings.c` 等文件里的文本是手工断好的，直接改写即可。

**5. 同一句英文只有一个译文**

翻译表按原文匹配、全局生效。`"Bag"` 在 `src/strings.c` 里出现 3 次
（`gText_MenuBag`、`gPCText_Bag` 等），它们共用同一条译文。所以翻译时要注意
这句话在**所有**出现场合都说得通。

要确认某个词还出现在哪些文件：

```bash
python3 tools/translation_check.py --impact
```

**6. 不要翻译的情况**

- 图鉴搜索的字母分组 `ABC`~`VWZ`：这是索引标签，必须保持拉丁字母
- 小数点用的 `"."`：`gText_DecimalPoint` 等，译成 `。` 会显示错误
- 纯占位符串（如 `"{STR_VAR_1}"`）：没有可译内容

### 新增汉字

字库目前还剩 **140 个空位**（码位 `0x1E6B`–`0x1EF6`），在这个范围内加字**不需要改任何代码**。

1. 在 `charmap.txt` 的中文区（`@Chinese char` 之后）末尾追加一行，
   码位接着往下排：

   ```
   '龘' = 1E6B
   ```

2. 用 `tools/generate_font.py` 重新生成字库图：

   ```bash
   python3 tools/generate_font.py
   ```

   注意：该脚本中的字体路径是为原作者的 Windows 环境写的
   （`C:/Windows/Fonts/simsun.ttc` 等），需要改成自己系统上的中文字体路径。

3. `make -j$(nproc)`

超过 140 个字就需要扩展编码范围，那时还要改 `src/chinese_text.c` 里
`IsChineseChar` 的高位范围和 `DecompressGlyph_Chinese` 的索引换算。

---

## 四、原理与文件清单

### 机制

```
translations/zh_CN.txt   ← 译文数据（唯一需要维护的文件）
        │
        │ 编译期按「源文本」精确查表
        ▼
tools/preproc            ← 所有文本的唯一必经之路
        │
        ▼
     ROM
```

`preproc` 是所有文本的处理入口：C 文件走 `CPP | PREPROC | CC1`，
汇编文件走 `PREPROC | CPP | AS`，而 `_("...")` 与 `.string "..."`
最终都会汇入 `StringParser::ParseString()`。汉化只在这一个函数里加了查表，
所以覆盖全项目（含 `data/text/`、`data/maps/`、poryscript 生成的 `.inc`）。

行为：

| 情况 | 结果 |
|---|---|
| 表中有该原文 | 使用译文 |
| 表中没有 | 保持原文（新增文本自动回落英文） |
| 表文件不存在 | 完全不启用，等同于未打补丁 |

### 文件清单

**新增文件**（不会与上游冲突）

| 文件 | 作用 |
|---|---|
| `translations/zh_CN.txt` | 译文表，1665 条 |
| `tools/preproc/translation.{h,cpp}` | 翻译表的加载与查表 |
| `tools/translation_check.py` | 翻译表校验 |
| `include/chinese_text.h`、`src/chinese_text.c` | 汉字判定、字形解压、字宽 |
| `graphics/fonts/chinese_{small,normal}.png` | 中文字库图 |
| `tools/generate_font.py` | 由 charmap + 字体生成字库图 |

**改动的上游文件**（合并时的潜在冲突面）

| 文件 | 改动 |
|---|---|
| `tools/preproc/string_parser.cpp` | 在 `ParseString` 中挂钩查表 |
| `tools/preproc/preproc.cpp` | 启动时加载翻译表 |
| `tools/preproc/Makefile` | 加入新源文件 |
| `Makefile` | 让 `.o` 依赖翻译表；字体扩展名清理规则 |
| `charmap.txt` | 追加中文码表 |
| `gflib/text.c` | `RenderText` / `GetStringWidth` 识别汉字 |
| `src/fonts.c`、`graphics_file_rules.mk` | 字体扩展名 `.latfont` → `.fwlatfont` |
| `tools/gbagfx/*` | 支持半角/全角字库读写 |
| `src/naming_screen.c` | 命名界面支持双字节字符 |
| `.gitignore` | 字体扩展名 |
| `data/scripts/Rogue/Strings/poryscript_font_config.json` | 中文字宽（用于 poryscript 断行） |
| `src/new_game.c`、`src/rogue_debug.c`、`src/rogue_multiplayer.c`、`tools/mapjson/json11.cpp` | 构建阻塞修复，与汉化无关 |

### 贡献

欢迎提交译文改进。改 `translations/zh_CN.txt` 后请确认校验通过：

```bash
python3 tools/translation_check.py
```

校验项：字符集是否在 charmap 内、占位符与转义是否一致、名称类是否超字节上限、
是否存在陈旧条目。
